```mermaid
flowchart TD

subgraph DRGSystem["National DRG System"]
  M1[Patient Data Mgmt]
  M2[Case-based Costing]
  M3[DRG Grouper]
end

subgraph WHO["WHO Codification Service"]
  ICD11[ICD-11 API (REST)]
  ICHI[ICHI Codes]
end

DRGSystem -->|Code Lookup| WHO
WHO -->|Return Validated Codes| DRGSystem

note over DRGSystem,WHO: Secure API Layer (OAuth2/JWT/HTTPS)
