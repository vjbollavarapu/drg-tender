```mermaid
flowchart TD

subgraph DRGSystem["National DRG System"]
  M1[Patient Data Mgmt]
  M2[Case-based Costing]
  M3[DRG Grouper]
end

subgraph WHO["WHO Codification Service"]
  ICD11[ICD11 API REST]
  ICHI[ICHI Codes]
end

subgraph Security["Security Layer"]
  SEC[Secure API Layer - OAuth2 / JWT / HTTPS]
end

DRGSystem -->|Code Lookup| WHO
WHO -->|Validated Codes| DRGSystem
DRGSystem --> SEC
WHO --> SEC
